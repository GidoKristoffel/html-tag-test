import { BracketTextDirective } from './bracket-text.directive';

describe('BracketTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BracketTextDirective();
    expect(directive).toBeTruthy();
  });
});
