import { MaxInputSizeDirective } from './max-input-size.directive';

describe('MaxInputSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxInputSizeDirective();
    expect(directive).toBeTruthy();
  });
});
